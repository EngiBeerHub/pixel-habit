import { TokenUpdateScreen } from "../../../features/settings/token-update-screen";

/**
 * Settings内のトークン変更画面ルート。
 */
export default function SettingsTokenRoute() {
  return <TokenUpdateScreen />;
}
