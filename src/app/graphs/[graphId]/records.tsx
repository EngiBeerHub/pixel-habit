import { GraphRecordListScreen } from "../../../features/graphs/graph-record-list-screen";

/**
 * グラフ記録一覧画面のルート。
 */
export default function GraphRecordListRoute() {
  return <GraphRecordListScreen />;
}
