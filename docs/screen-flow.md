# 画面遷移マップ（UX再設計）

このドキュメントは、MVP優先で実装可能なUX方針を含む遷移設計です。

## 前提

- 対象: iOS / Android（Expo + React Native）
- タブ構成: `Home` と `Settings` の2タブ
- 未ログイン時は認証ハブを表示

## 画面一覧

1. `認証ハブ`
- ルート案: `/auth`
- 役割: `ログイン` / `アカウント作成` の入口

2. `ログイン`
- ルート案: `/auth/sign-in`
- 役割: `username` / `token` 入力で利用開始

3. `アカウント作成`
- ルート案: `/auth/sign-up`
- 役割: Pixelaユーザー作成

4. `Homeタブ`
- ルート案: `/home`
- 役割: グラフカード一覧、追加、更新、削除、統計、記録操作

5. `ピクセルクイック追加シート`
- ルート案: Home内の Bottom Sheet
- 役割: 当日中心の素早い記録追加

6. `ピクセル詳細入力画面`
- ルート案: `/graphs/[graphId]/add`
- 役割: 日付指定を含む詳細な記録追加

7. `Settingsタブ`
- ルート案: `/settings`
- 役割: ユーザー設定、外部リンク、危険操作

## HomeタブのUI方針

- 上部に `Compact / Full` 切替
- グラフカードは主アクションを `記録する` のみに固定
- `編集 / 統計 / 削除` は `...` メニューに集約
- 画面右下に `グラフ追加` FAB
- pull-to-refresh を提供
- グラフの作成・編集ではカラーテーマを選択し、カードUIとヒートマップ色へ反映する

## Pixel追加UX方針

1. 標準操作:
- Homeカードの `記録する` で Bottom Sheet を開く
- 初期値は「今日」、入力項目は `date` と `quantity` のみ
- 保存成功時はシートを閉じ、カード内容を更新し、Home上部にToastで通知する

2. 詳細操作:
- シートから `詳細入力` で `/graphs/[graphId]/add` へ遷移
- 日付変更や詳細入力はこの画面で対応

3. 削除など危険操作:
- すべて確認ダイアログ必須

## 遷移フロー（推奨）

1. 未ログイン時は `認証ハブ` 表示
2. `ログイン` または `アカウント作成` 成功で `Home` へ
3. Homeで `記録する` 押下
4. Bottom Sheet でクイック追加
5. 必要時のみ `詳細入力画面` へ遷移
6. `Settings` からプロフィール/規約/ログアウト等へ遷移

## ヒートマップ方針

- MVP:
  - `Compact`: 自前ヒートマップ（14週=98日、5段階: 薄灰+テーマ色4段階）
  - `Compact` の描画データはグラフカード単位で `GET /v1/users/{username}/graphs/{graphID}/pixels` を取得する
  - `Compact` は GitHub 風に、上部へ月境界ラベル・左側へ `Mon / Wed / Fri` ラベルを表示する
  - `Full`: 暫定でPixela画像を許容
- MVP後:
  - `Full` も自前描画へ統一
- カラーテーマ方針:
  - Pixelaの `shibafu / momiji / sora / ichou / ajisai / kuro` をUIテーマ候補として表示
  - 選択した色は、グラフカードのアクセント色とヒートマップ描画色に統一して反映する

## TODO（要判断）

- `Others` の名称を `Settings` に変更するか（推奨は変更）
- Homeの成功Toast表示時間を将来カスタマイズ可能にするか

## 更新ルール

- 画面追加・削除・名称変更時は本ファイルを同コミットで更新する
- UX方針変更時は `HomeタブのUI方針` と `Pixel追加UX方針` を同時更新する

## 共通UI適用メモ

- `auth` / `settings` / `graphs` の主要画面は、`src/shared/ui/` の共通コンポーネントへ段階適用する
- 共通化後も、本ドキュメントで定義した画面導線と遷移順は変更しない
- テスト導入（Jest/RNTL/Maestro）は品質担保を目的とし、画面遷移そのものは変更しない
